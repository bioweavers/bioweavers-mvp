name: bioweavers-gdf
channels:
  - conda-forge
dependencies:
  - python=3.11
  # geospatial C-stack — pin majors to avoid ABI mismatches
  - geopandas>=1.0
  - shapely>=2.0
  - pyproj==3.7.2
  - pyogrio>=0.7
  - proj=9.8.1
  - proj-data # forces environment to bundle projection data files, needed for pyproj to work in the container
  # data + viz
  - pandas
  - numpy
  - matplotlib
  - plotly
  - pydeck
  # web
  - streamlit
  - streamlit-folium
  - fastapi
  - uvicorn
  - requests
  # docs
  - python-docx
  - docxtpl
  - openpyxl
  - setuptools<78  # docxcompose uses removed pkg_resources
  # dev
  - jupyter
  - notebook
  - ipykernel
  - debugpy
  - pytest
  